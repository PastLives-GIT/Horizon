### 1. 顶部导航栏 (Header / Navbar)
**设计目标**：极简、不干扰阅读，提供核心导航和全局设置。
*   **布局与层级**：
    *   **高度**：固定 `64px`。
    *   **背景**：半透明 + 毛玻璃效果 (`backdrop-blur-md`)，向下滚动时出现极细的底部阴影或分割线。
    *   **桌面端 (Desktop)**：三栏布局。左侧 Logo | 中间导航链接 | 右侧全局操作。
    *   **移动端 (Mobile)**：两栏布局。左侧 Logo | 右侧汉堡菜单 (Hamburger Icon)。点击后弹出全屏抽屉或侧边栏。
*   **组件拆解**：
    *   `Logo`：左侧，一个小巧的 Horizon 图标 + 粗体字 "Horizon"（不要太大，高度约 24px）。
    *   `NavLink`：今日速递 | 历史归档 | 订阅 | 关于。当前页面的链接底部带有品牌色下划线，Hover 时文字颜色变深。
    *   `LangSwitcher`：极简的 `中 / EN` 切换按钮，或者一个小的地球图标下拉菜单。
    *   `ThemeToggle`：太阳/月亮图标，点击平滑切换深浅色模式。

### 2. 首屏：今日速递 (Hero / Today's Edition) 🌟 核心区域

**设计目标**：这是用户到达后看到的第一屏，需要产生视觉冲击力。用 3D 透视卡片墙（WonderHTML5Wall 效果）展示当日精选新闻——鼠标移动时卡片跟随产生流体般的透视变形（CSS matrix3d），hover 时滑出文章信息。在卡片墙下方用一行概要文字总结"今日看点"，让用户 3 秒内理解今天的新闻全貌。

*   **布局与层级**：
    *   **背景**：深色径向渐变（#444 → #222），让彩色卡片在暗背景上"浮起"，营造沉浸感和空间深度。Hero 区域顶部与上方导航栏之间无分割线，自然过渡。
    *   **桌面端 (Desktop)**：卡片墙居中，4 列网格，约 12-16 张卡片（按当日文章数动态调整）。每张卡片约 220px × 180px。
    *   **移动端 (Mobile)**：2 列网格，卡片等比缩小至约 150px × 130px，降低 GPU 负担同时保持可触达性。
    *   **3D 透视引擎**：
        *   网格交点（Point）在静止时回到原始位置，形成规整的矩形网格。
        *   鼠标进入 Wall 区域后，每个交点根据与鼠标的距离被向外推开（距离越近推力越大，超出影响范围则弹簧回弹）。
        *   每张卡片的 4 个角点分别计算 matrix3d 变换，产生真实的透视变形（参考 PerspectiveTransform.js 算法）。
        *   所有动画通过 requestAnimationFrame 驱动，60fps 流畅运行。
        *   页面滚动和窗口 resize 时正确更新偏移量。
    *   **入场动画**：所有卡片初始位置聚合在屏幕中心，然后弹簧动画展开到各自的网格位置（"爆炸-归位"效果）。完成后 Wall 容器 opacity 从 0 过渡到 1。

*   **组件拆解**：
    *   `WallCard` (3D 透视卡片，替代原 WonderHTML5Wall 的 Flickr 照片)：
        *   **视觉层** (wall-card-visual)：100% 填充。**首选**：当日该语言下最高分文章的 OG 图（Open Graph image）作为 `background-image`，`background-size: cover`，`background-position: center`。图片上方叠加一层半透明渐变遮罩（`rgba(0,0,0,0.35)` 或品牌色 30% 透明度），确保白色文字清晰可读。**回退**：当没有 OG 图时，使用 Horizon 品牌对角渐变色——奇数卡片：`var(--hz-accent)` → `var(--hz-indigo)`；偶数卡片：`var(--hz-accent-alt)` → `var(--hz-rose)`。中央大字显示日期（如 "07-24"，2em，font-weight 800），下方依次显示语言徽章（EN / 中文 的小胶囊，border: 1px solid rgba(255,255,255,.35)）和文章数量（如 "13 items"，0.78em，opacity 0.75）。
        *   **悬停交互**：鼠标悬停时视觉层轻微放大（scale(1.05)），同时阴影加深。
        *   **信息层** (wall-card-info)：初始位于卡片底部外侧（top: 100%），即完全隐藏。背景色与视觉层呼应（奇数卡用 --hz-accent，偶数卡用 --hz-accent-alt）。内容包含：标题（粗体 15px，最多 2 行）、AI 评分徽章（score-badge，红/橙/金/灰四档）、标签小点（tag-dot，半透明白底圆角胶囊）。
        *   **悬停滑出**：鼠标悬停时信息层 top 过渡到 55%，向上滑出覆盖卡片下半部分。
        *   **整卡可点击**：用 <a> 包裹整张卡片，点击跳转到对应文章页。
    *   `TodaysBrief` (今日概要条)：
        *   位于卡片墙下方 24px，居中一行紧凑文字。
        *   格式如："📅 2026年7月24日 · 从 15 条中精选 13 条 · 来源：Hacker News · 最高分 8.0 🔥"
        *   字体 0.9em，颜色 muted（var(--hz-text-muted)），作为卡片墙与下一屏之间的视觉过渡。

### 3. 次屏：更多雷达发现 (The Feed / More Discoveries)
**设计目标**：高效浏览中低分数但仍有价值的新闻，提供分类过滤。
*   **布局与层级**：
    *   **标题区**：左侧大标题 "更多雷达发现"，右侧可选的 "查看全部" 链接。
    *   **筛选栏 (Tabs)**：标题下方，横向滚动的胶囊按钮 (Pills)。`全部` | `AI` | `开源` | `产品` | `Web3`。选中状态为黑底白字（浅色模式）或白底黑字（深色模式）。
    *   **内容区**：单列列表布局 (List View)，比网格更节省垂直空间，适合快速扫读。
*   **组件拆解**：
    *   `FeedItem` (信息流列表项)：
        *   **布局**：左侧占 80%（文本），右侧占 20%（元数据/操作）。
        *   **左侧**：`SourceBadge` + 标题 (H4，单行省略) + 一句话摘要 (单行省略)。
        *   **右侧**：AI 评分 (小字号) + 外部链接图标 (Hover 时高亮)。
        *   **分割线**：每个 Item 底部有极细的浅灰色分割线。
        *   **交互**：Hover 时整行背景色微变（如变为 `#F9FAFB`）。

### 4. 第三屏：订阅与连接 (Subscribe & Connect)
**设计目标**：将访客转化为长期读者，提供无缝的订阅体验。
*   **布局与层级**：
    *   **背景**：使用与页面主背景不同的颜色（如浅灰或品牌色的极浅色调），形成一个视觉上的“区块 (Section)”，突出重要性。
    *   **布局**：居中卡片设计，最大宽度 `800px`。
*   **组件拆解**：
    *   `SectionTitle`：大标题 "让 Horizon 每天为你过滤噪音"，副标题 "选择你最喜欢的接收方式"。
    *   `SubscribeTabs` (订阅选项卡)：
        *   **邮件 (Email)**：一个输入框 + "订阅" 按钮，横向排列。输入框带 Focus 发光效果。下方小字提示 "我们讨厌垃圾邮件，随时可退订"。
        *   **RSS**：一个只读输入框（显示 RSS 链接）+ 右侧 "一键复制" 图标按钮。点击后图标变为 "✅ 已复制" 并持续 2 秒。
        *   **Webhook**：三个小图标按钮（飞书、钉钉、Slack），点击弹出 Modal 或跳转到配置文档。

### 5. 第四屏：关于 Horizon (Under the Hood)
**设计目标**：向好奇的读者解释“这些内容是怎么来的”，建立信任感，并引导开发者去 GitHub。
*   **布局与层级**：
    *   **桌面端**：左右 50/50 分栏。左侧文案，右侧视觉。
    *   **移动端**：上下堆叠，先视觉后文案。
*   **组件拆解**：
    *   **左侧 (文案)**：
        *   标题："不只是摘要，而是你的 AI 新闻雷达"。
        *   正文：简短介绍 Horizon 的核心理念（保留人的品味、多源整合、背景补充）。
        *   CTA 按钮：`⭐ 在 GitHub 上 Star` (主按钮) | `📖 阅读配置文档` (次按钮/幽灵按钮)。
    *   **右侧 (视觉)**：
        *   `PipelineGraphic` (极简流程图)：不要用复杂的架构图。用 3-4 个带有图标的圆角卡片，用虚线箭头连接。
        *   *节点*：`📡 多源抓取` -> `🤖 AI 评分去重` -> `🔍 背景补充` -> `📰 生成简报`。

### 6. 页脚 (Footer)
**设计目标**：提供兜底导航，展示开源属性和社区支持。
*   **布局**：
    *   顶部：一条分割线。
    *   中间：多列布局（项目链接、社区链接、法律/协议）。
    *   底部：版权信息、MIT 协议、"Powered by Horizon" 小字。
*   **组件**：
    *   `SponsorLogos` (可选)：如果 README 中提到了支持者（如 LINUX.DO, HelloGitHub），在这里用灰度 Logo 展示，Hover 时变彩色。

---

### 🎨 关键 UI/UX 细节补充

1.  **来源标签 (Source Badge) 设计规范**：
    这是提升“信息丰富度”的关键。每个 Badge 应该是一个小胶囊，包含小图标和文字，背景色为对应品牌色的 10% 透明度，文字为 100% 品牌色。
    *   `Hacker News`：橙色 (背景 `#FFF7ED`，文字 `#C2410C`)
    *   `Reddit`：橙红色 (背景 `#FFF1F2`，文字 `#BE123C`)
    *   `GitHub`：深灰/黑色 (背景 `#F3F4F6`，文字 `#1F2937`)
    *   `RSS`：绿色 (背景 `#ECFDF5`，文字 `#047857`)
    *   `Twitter/X`：黑色/蓝色
2.  **AI 评分可视化**：
    不要只显示干巴巴的数字。
    *   **高分 (8-10)**：使用 🔥 火焰图标 + 红色/橙色数字，或者带一个满格的微型进度条。
    *   **中分 (5-7)**：使用 💡 灯泡图标 + 黄色/蓝色数字。
    *   **低分 (不展示或折叠)**：如果是低分新闻，建议直接在网页端过滤掉，保持内容的高质量。
3.  **骨架屏 (Skeleton Loading)**：
    虽然是静态站点，加载极快，但在首次加载图片或切换语言时，使用带有微光动画的灰色色块（Skeleton）作为占位符，体验会比白屏好很多。

---

### 数据契约 (Data Contract)

`manifest.json` 中每篇文章项需新增 `og_image` 字段，用于卡片墙的背景填充：

```typescript
interface ManifestItem {
  title: string;
  score: number;
  tags: string[];
  source_type: string;
  summary: string;
  og_image: string | null;  // NEW: 文章的 Open Graph 图片 URL，null 时回退到品牌渐变
}
```

Horizon 需要在 enrichment 阶段抓取文章 URL 的 OG 元数据（`<meta property="og:image">`），存入 `ContentItem.metadata["og_image"]`，`SiteBuilder` 生成 manifest 时写入此字段。

### 下一步建议

现已确定 OG 图为卡片首选填充内容。如果前端先开发，可以用占位图测试——先实现渐变回退逻辑，OG 图字段就绪后直接生效。需要我设计完整的 `manifest.json` 数据结构吗？